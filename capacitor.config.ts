import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'trabajo.u1',
  appName: 'trabajo-u1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
